extends ../wrapper

block seo
	title.
		Register

block content
	h3.
		Register
	form.form.form-inline(action="/register" method="POST")
		input(type="text" name="email" placeholder="Email address")
		input(type="password" name="password" placeholder="Password")
		input.btn.btn-primary(type="submit")
